# Since we’re not following the standard layout of a GNU project, we warn
# automake that this is a foreign project
AUTOMAKE_OPTIONS = foreign

lib_LTLIBRARIES = liblinkjacrypto.la

liblinkjacrypto_la_SOURCES = src/linkja-crypto.c

include_HEADERS = src/include/*.h
